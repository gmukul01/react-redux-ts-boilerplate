export interface Button {
    flat: {
        primaryColor: string;
    };
    outlined: {
        contrasColor: string;
        primaryColor: string;
    };
    solid: {
        contrasColor: string;
        primaryColor: string;
        secondaryColor: string;
    };
}
