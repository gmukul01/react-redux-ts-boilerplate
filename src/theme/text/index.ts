import colors from '../colors';
import { TextTheme } from './types';

const text: TextTheme = {
    defaults: {
        textSize: 'M2',
        textWeight: 'Normal',
        textColor: colors.black
    }
};

export default text;
